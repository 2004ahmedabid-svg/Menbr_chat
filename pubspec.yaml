name: menbr_chat
description: تطبيق مراسلة كامل - حقوقي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  file_picker: ^8.3.2
  agora_rtc_engine: ^6.3.0
  flutter_local_notifications: ^17.2.1
  local_auth: ^2.1.8
  encrypt: ^5.0.3

flutter:
  uses-material-design: true
